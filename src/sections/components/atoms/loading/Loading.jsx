import React from 'react'
import './loading.css'


// Loading component that handles the loading animation -> (Loading...)
const Loading = () => {
  return (
    <div className='loading_container'>Loading...</div>
  )
}

export default Loading